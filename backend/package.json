{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "dev": "bun run --watch src/index.ts",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:migrate:create": "bun run src/db/create-migration.ts"
  },
  "dependencies": {
    "@elysiajs/swagger": "^1.3.1",
    "elysia": "latest",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
